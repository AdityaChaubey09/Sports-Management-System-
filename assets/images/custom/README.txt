Put your custom images in this folder using these exact names:

- home-hero.jpg : home page "Play Beyond Limits" hero background
- logo.png : navbar logo shown on every page

If either custom file is missing, the app uses its built-in stock fallback image.
